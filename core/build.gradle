apply plugin: 'java-library'

ext {
    artifact = 'prefs-builder-core'
    libraryName = 'prefs-builder-core'
    libraryDescription = 'The Core of Prefs Builder'
    libraryVersion = '0.1.0'
}

dependencies {
}

sourceCompatibility = 1.8

apply from: '../publish.gradle'
