apply plugin: 'java-library'

version = publishVersion
archivesBaseName = 'prefs-builder-core'
group = publishGroupId

ext {
    libraryName = 'PrefsBuilder-core'
    libraryDescription = 'the Core of Prefs Builder'
}

dependencies {
}

sourceCompatibility = 1.8

apply from: '../publish.gradle'
